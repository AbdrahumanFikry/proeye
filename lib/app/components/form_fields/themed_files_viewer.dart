import 'package:flutter/material.dart';

class ThemedFilesViewer extends StatelessWidget {
  final List<String> fileUrls;

  const ThemedFilesViewer({
    Key key,
    @required this.fileUrls,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
